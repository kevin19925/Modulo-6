class Direccion {
  String calleP;
  String calleS;
  String? referencia;

  Direccion(this.calleP, this.calleS, this.referencia);
}

void main() {
  Direccion dir = Direccion("", "", null);
}
